/**
 * The atob() method decodes a base-64 encoded string.
 * @param encodedStr The string which has been encoded by the btoa() method.
 * @returns A String, representing the decoded string.
 */
declare function atob(encodedStr: string): string;